\clearpage
\TFMChapter{Implementación}

\TFMSection{Introducción}
Mientras que el Capítulo 3 describía qué se ha diseñado y por qué, este capítulo describe cómo se ha construido cada componente de la solución. El objetivo de esta descripción no es únicamente documentar el código, sino explicar qué función cumple cada pieza dentro del proceso de generación de una propuesta de viaje y qué valor aporta al profesional que la utiliza: dónde se automatiza una tarea manual, dónde se reduce el riesgo de error o de información desactualizada, y dónde se mantiene deliberadamente el control humano.

\TFMSection{Estructura del Código}
El proyecto se organiza como un monorepo con cuatro grandes bloques, correspondientes a los cuatro componentes descritos en el capítulo anterior:

\begin{verbatim}
src/                  Orquestador multiagente (LangGraph)
  graph.py              Definición del grafo: nodos, aristas, interrupciones
  nodes.py              Identificadores de cada nodo del grafo
  state.py              Estado compartido entre agentes
  state_persistence.py  Serialización del estado para su persistencia
  agents/               Un módulo por agente especializado
  clients/              Clientes a RAG, backend, almacenamiento y precios

front/                Interfaz de usuario (Streamlit)
  views/                Pantallas de conversación y de histórico

backend_api/          API de persistencia (FastAPI)
  routers/              Endpoints de autenticación, documentos y propuestas
  repositories/         Acceso a la base de datos

scripts/aws-lambda/   Pipeline de procesamiento documental (AWS Lambda)
  pdf_to_image/         Conversión de PDF a imágenes
  image-to-markdown/    Extracción de contenido y metadatos
  index-markdown/       Fragmentación e indexación

templates/            Plantillas corporativas de la propuesta (PPTX)
\end{verbatim}

Esta organización mantiene separadas cuatro responsabilidades que, aunque colaboran para producir una única propuesta de viaje, tienen ciclos de vida y necesidades de despliegue distintas: el pipeline documental se ejecuta de forma asíncrona y esporádica (solo cuando se carga un documento nuevo), mientras que el orquestador y la interfaz atienden conversaciones en tiempo real, y la API de persistencia da servicio a ambos por igual.

\TFMSection{Implementación del pipeline de procesamiento documental}

El pipeline transforma automáticamente los documentos PDF proporcionados por los
proveedores en contenido estructurado e indexado, preparado para su consulta mediante
lenguaje natural. Se ha implementado siguiendo una arquitectura \textit{serverless} y
orientada a eventos, de forma que el procesamiento comienza automáticamente cuando
se incorpora un nuevo documento al repositorio de entrada.

Para ello, se ha configurado una regla de Amazon EventBridge que detecta los eventos
de creación de objetos con extensión \texttt{.pdf} en el bucket de Amazon S3
\texttt{tfm\_raw\_documents}. Cuando se produce un evento que cumple estas condiciones,
la regla inicia una ejecución de la máquina de estados de AWS Step Functions y le
proporciona la referencia al bucket y a la clave del documento cargado. De este modo,
el usuario únicamente debe depositar el PDF en el repositorio correspondiente, sin
necesidad de iniciar manualmente ninguna función ni proceso adicional.

La máquina de estados ejecuta secuencialmente tres funciones Lambda independientes:
\texttt{PdfToImage}, \texttt{ImageToMarkdown} e
\texttt{IndexDocumentEmbeddings}. Cada función conserva sus resultados en los
servicios de almacenamiento o persistencia correspondientes y transmite a la siguiente
etapa únicamente las referencias necesarias para continuar el procesamiento, evitando
incluir los documentos completos en el estado del flujo.

\newpage
La Figura~\ref{fig:step-functions-document-pipeline} muestra la orquestación del
pipeline y los servicios con los que interactúa cada función Lambda.

\begin{figure}[!ht]
    \centering
    \includegraphics[width=0.95\textwidth]
    {assets/diagrama_pipeline_procesamiento_documental.png}
    \caption{Orquestación e interacciones del pipeline documental implementado mediante AWS Step Functions.}
    \label{fig:step-functions-document-pipeline}
\end{figure}

\begin{tfmbullets}

\item \textbf{\texttt{PdfToImage}.}
La primera función recibe la referencia al documento PDF almacenado en Amazon S3 y
renderiza cada una de sus páginas como una imagen de alta resolución mediante la
librería PyMuPDF. Esta conversión permite conservar tablas, columnas, logotipos,
precios y otros elementos visuales que podrían perderse mediante una extracción
convencional de texto.

Las imágenes resultantes se almacenan nuevamente en Amazon S3, dentro del repositorio
destinado a las páginas procesadas, y sus claves se incorporan al estado de la ejecución
para que puedan ser utilizadas por la siguiente etapa. Esta función interactúa, por
tanto, principalmente con Amazon S3, desde donde obtiene el PDF original y donde
almacena las imágenes generadas.

\item \textbf{\texttt{ImageToMarkdown}.}
La segunda función recupera de Amazon S3 las imágenes generadas y las procesa mediante
un modelo multimodal con capacidad de visión. El modelo reconstruye el contenido del
documento en formato Markdown, conservando su jerarquía de encabezados, tablas,
condiciones comerciales, precios y demás información relevante.

Cuando se detectan enlaces web dentro del documento, la función consulta dichas
páginas e incorpora al Markdown la información complementaria obtenida mediante
\textit{web scraping}. A continuación, se realiza la limpieza y normalización del
contenido y se extraen de forma estructurada los metadatos del documento, como el
destino, el proveedor, las actividades, los precios, las condiciones y los campos de
accesibilidad.

El proceso incluye reglas para normalizar topónimos con diferentes denominaciones y
para evitar la generación de datos de contacto que no aparezcan explícitamente en las
fuentes procesadas. El Markdown resultante se almacena en Amazon S3, mientras que los
metadatos estructurados se envían a la API backend, que se encarga de persistirlos en
Amazon DynamoDB.

Esta función interactúa con Amazon S3, el proveedor del modelo multimodal, las páginas
web referenciadas en el documento y la API de persistencia de la aplicación.

\item \textbf{\texttt{IndexDocumentEmbeddings}.}
La última función recupera de Amazon S3 el Markdown normalizado y obtiene, cuando es
necesario, los metadatos asociados al documento a través de la API backend. El contenido
se divide en fragmentos de tamaño acotado, procurando mantener unidades de información
suficientemente completas para su posterior recuperación.

Para cada fragmento se genera una representación vectorial mediante el modelo Titan
Text Embeddings de Amazon Bedrock. Los fragmentos, sus vectores y los metadatos
relacionados con el destino y las actividades se indexan posteriormente en Amazon
OpenSearch Serverless.

Esta función interactúa con Amazon S3 para recuperar el contenido, con la API backend
para consultar los metadatos, con Amazon Bedrock para generar los embeddings y con
Amazon OpenSearch Serverless para almacenar los fragmentos indexados.

\end{tfmbullets}

AWS Step Functions controla el orden de ejecución y garantiza que cada función se
inicie únicamente cuando la etapa anterior ha finalizado correctamente. La persistencia
de los resultados intermedios en Amazon S3 desacopla las funciones y permite probar,
actualizar o volver a ejecutar cada una de ellas de manera independiente.

Una vez finalizada la función \texttt{IndexDocumentEmbeddings}, el contenido del
documento queda disponible en Amazon OpenSearch Serverless para las consultas
realizadas por el sistema RAG. De esta forma, el proceso completo, desde la carga del
PDF hasta su disponibilidad para la recuperación semántica, se ejecuta automáticamente
sin intervención manual.

\TFMSection{Implementación del sistema RAG}

El sistema RAG permite fundamentar las respuestas en la documentación real de los
proveedores, reduciendo el riesgo de generar información inventada, desactualizada o no relacionada con el destino solicitado.

La base de conocimiento se implementa sobre Amazon OpenSearch Serverless. Cada
fragmento documental se almacena junto con su representación vectorial, su contenido textual y los metadatos extraídos durante el pipeline, como la ciudad, la provincia, el país y las actividades asociadas.

Cuando un agente realiza una consulta, esta se transforma en un embedding mediante el modelo Titan Text Embeddings de Amazon Bedrock. A continuación, el cliente RAG ejecuta en OpenSearch Serverless una búsqueda \textit{k-nearest neighbours} (k-NN), combinada con filtros exactos de destino y provincia. De esta forma, se recuperan los fragmentos semánticamente más próximos sin mezclar documentación correspondiente a otras localizaciones.

La Figura~\ref{fig:rag-query-flow} resume el flujo de consulta implementado, desde la generación del embedding hasta la construcción del contexto enviado al modelo de lenguaje.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\textwidth]
    {assets/flujo_consulta_sistema_rag.png}
    \caption{Flujo simplificado de consulta del sistema RAG.}
    \label{fig:rag-query-flow}
\end{figure}

Las comunicaciones con Amazon Bedrock y Amazon OpenSearch Serverless se realizan
mediante credenciales y permisos de AWS IAM. Las peticiones a OpenSearch Serverless se firman mediante AWS Signature Version 4, evitando utilizar credenciales específicas de la base de datos dentro de la aplicación.

Tras recuperar los resultados, se aplica un umbral mínimo de relevancia para descartar los fragmentos con baja similitud. Los fragmentos válidos se formatean junto con sus metadatos y se incorporan al contexto del modelo de lenguaje para generar una respuesta fundamentada.

El sistema dispone también de un módulo de \textit{reranking} basado en Amazon
Bedrock. Aunque está implementado y es funcional, no se invoca actualmente en la ruta activa de consulta, a la espera de evaluar su impacto en la calidad, la latencia y el coste.

Los principales consumidores del sistema RAG son el agente de información, que recupera contexto cultural y actividades, y el agente de precios, que lo utiliza de forma complementaria para localizar alojamientos y servicios incluidos en la documentación de los proveedores.

\TFMSection{Implementación del sistema multiagente}
\label{sec:impl-multiagente}

El sistema multiagente se implementa como un grafo de estados en LangGraph, cuyos nodos y flujos de control se resumen en la Figura \ref{fig:orquestrador}. A continuación se describe la función que cumple cada agente dentro del grafo y las decisiones de implementación adoptadas en cada caso.


\subsection{Agente orquestador}
Es el punto de entrada de cada conversación y el responsable de que el sistema entienda qué viaje se está solicitando antes de empezar a trabajar. Analiza cada mensaje del profesional y extrae de forma estructurada los datos necesarios (cliente, origen, destino, fechas e intereses), solicitando una aclaración cuando falta información en lugar de generar una propuesta con datos incompletos. Una decisión de diseño relevante para la experiencia de uso es que, si el profesional cambia de destino a mitad de la conversación, el agente descarta automáticamente la información y los precios ya recopilados para el destino anterior, evitando así que la propuesta final mezcle datos de dos viajes distintos; si en cambio solo cambian las fechas, conserva el contexto cultural ya recuperado y solo recalcula los precios.

\begin{figure}[h!]
\centering
\includegraphics[width=\textwidth,height=0.88
\textheight,keepaspectratio]{assets/diagrama_orquestrador.png}
\caption{Diagrama del orquestador multiagente.}
\label{fig:orquestrador}
\end{figure}


\subsection{Agente de información}
Transforma la documentación turística indexada en un relato legible: recupera, para el destino y cada uno de los intereses expresados por el cliente, los fragmentos más relevantes de la base documental, descarta los que no superan un umbral mínimo de relevancia y sintetiza el resultado en un contexto cultural y un listado de actividades recomendadas. El valor de este agente es doble: evita que el profesional tenga que buscar manualmente en los documentos de los proveedores, y fundamenta la respuesta en contenido real en lugar de en conocimiento genérico del modelo.

\subsection{Agente de precios}
Combina dos fuentes de información para presentar opciones de vuelo y alojamiento: un servicio externo que consulta precios de mercado en tiempo real, y la propia base documental, para alojamientos concretos que solo aparecen en los catálogos de los proveedores. La selección de candidatos (los vuelos más económicos y los alojamientos mejor valorados) se calcula de forma determinista en código, no mediante el modelo de lenguaje, lo que garantiza que el orden de las opciones presentadas sea siempre coherente y verificable. El resultado no se aplica automáticamente: se presentan varias opciones de cada categoría para que el profesional elija (Figura~\ref{fig:frontend-seleccion-precios}), manteniendo la decisión comercial en manos humanas.

\subsection{Generación del itinerario}
A partir de la información turística recuperada, los precios seleccionados y las preferencias expresadas durante la conversación, se genera un itinerario día a día. Varios cálculos que podrían delegarse en el modelo de lenguaje se resuelven deliberadamente en código para garantizar su exactitud: la duración del viaje se calcula a partir de las fechas, y los horarios de los vuelos de llegada y salida se derivan de los precios seleccionados y se incorporan como instrucción al itinerario, en lugar de dejar que el modelo interprete horas de vuelo en formato libre. El itinerario también respeta un límite de extensión calculado a partir del espacio real disponible en la plantilla de la propuesta, de forma que el texto generado encaje siempre en el documento final sin necesidad de retoque manual.

\subsection{Generación de la propuesta comercial}
La propuesta final combina dos elementos generados de forma independiente: una imagen de portada asociada al destino y a las actividades seleccionadas, generada mediante un modelo de generación de imágenes \parencite{openai-images-2025}, y el propio itinerario. Ambos se integran en una plantilla corporativa en formato PPTX ya existente, editable por el profesional, sustituyendo los textos e imágenes de la plantilla por el contenido generado y ajustando dinámicamente el número de diapositivas del itinerario según la duración del viaje. El valor de este agente es que entrega un documento comercial ya maquetado y listo para revisar o enviar, sin requerir intervención de diseño.

\subsection{Gestión del estado y transiciones}
Todos los agentes anteriores comparten un mismo estado, que conserva los parámetros del viaje, la información recuperada, los precios, el itinerario y el resultado de cada etapa. Este estado se conserva de forma persistente cada vez que el flujo se detiene para una decisión humana, lo que permite que un profesional pueda interrumpir la elaboración de una propuesta y retomarla más adelante, incluso desde otra sesión, sin perder el trabajo ya realizado por el sistema. Las cuatro acciones disponibles en la pantalla de revisión final (Figura~\ref{fig:frontend-revision-flyer}) --aprobar, rechazar, descartar o guardar como borrador-- se corresponden directamente con los valores que puede tomar el estado de revisión de la propuesta.

Cabe señalar, como observación técnica honesta, que el mecanismo de \textit{checkpointing} en memoria (\texttt{MemorySaver}) utilizado actualmente no conserva la posición exacta del \texttt{interrupt} del grafo cuando el proceso se reinicia: al recrearse el grafo, \texttt{current\_state.next} ya no permite distinguir en qué punto de revisión humana se encontraba una propuesta restaurada. Como mitigación, la interfaz incorpora una heurística (\texttt{\_at\_human\_review}, \texttt{\_at\_pricing\_selection}) que infiere el punto de interrupción a partir del propio contenido del estado persistido (por ejemplo, presencia de opciones de precio candidatas sin selección final, o de un borrador de propuesta ya redactado) cuando la posición del \texttt{interrupt} no está disponible. Esta heurística resuelve el caso práctico de continuar una propuesta tras un reinicio del contenedor, pero no sustituye a una solución de fondo: la persistencia externa del estado conversacional interrumpido se mantiene como línea de trabajo futuro prioritaria (Capítulo 8).

\TFMSection{Observabilidad y trazabilidad (LangSmith)}
Para dar cumplimiento al requisito RNF7 (Trazabilidad), se ha incorporado \textbf{LangSmith} como plataforma de observabilidad del orquestador multiagente, integrada mediante un módulo propio (\texttt{src/observability.py}) que actúa como capa fina sobre el trazado automático que LangChain y LangGraph ya ofrecen.

La integración se ha diseñado para que sea completamente opcional y no invasiva: el trazado solo se activa cuando existen simultáneamente la variable \texttt{LANGSMITH\_TRACING=true} y una \texttt{LANGSMITH\_API\_KEY} válida, de forma que un entorno local que copie el fichero \texttt{.env.example} sin rellenar la clave siga funcionando de forma silenciosa, sin generar avisos del SDK ni alterar el comportamiento del grafo. Cuando el trazado está desactivado, las funciones del módulo devuelven una configuración vacía, por lo que su uso en el resto del código es un no-op transparente.

Cuando el trazado está activo, cada ejecución del grafo se enriquece con información de negocio que, de otro modo, quedaría invisible en un panel de trazas genérico:

\begin{tfmbullets}
\item \textbf{Metadatos por propuesta:} cada ejecución se etiqueta con el identificador de la propuesta (\texttt{flyer\_id}) y el nombre del cliente, de forma que el panel de LangSmith permite localizar y filtrar las trazas de una conversación concreta en lugar de mostrar un flujo anónimo.
\item \textbf{Metadatos por agente:} cada llamada al modelo de lenguaje dentro de un agente se etiqueta adicionalmente con el nombre del propio agente (\texttt{agent:orchestrator}, \texttt{agent:pricing\_agent}, etc.), heredando las etiquetas y metadatos de la ejecución padre, lo que permite desglosar coste, tokens y latencia por agente dentro de una misma propuesta.
\item \textbf{Nombres de ejecución legibles:} cada traza recibe un nombre descriptivo (por ejemplo, \textit{``Flyer generation — Cliente X''}) en lugar del identificador interno del grafo, facilitando la inspección manual del panel durante el desarrollo y la evaluación del sistema.
\end{tfmbullets}

Esta capa se ha propagado a los agentes orquestador, de información, de precios y de generación de imagen, así como al propio grafo (\texttt{src/graph.py}), de forma consistente con el resto de agentes que ya utilizaban salida estructurada. La integración se acompaña de pruebas unitarias dedicadas (\texttt{tests/unit/src/test\_observability.py}) que verifican tanto el comportamiento no-op cuando el trazado está desactivado como el enriquecimiento correcto de metadatos y etiquetas cuando está activo \parencite{langsmith2026docs}.

\TFMSection{Integración con modelos de lenguaje}
Cada agente utiliza el modelo de lenguaje más adecuado a su tarea, atendiendo a un equilibrio entre coste y fiabilidad: modelos con cuota de uso económica para tareas de alto volumen (interpretación de la conversación, reconocimiento de documentos) y modelos con mayor capacidad de generación estructurada para tareas donde un error tiene más impacto en el resultado final (recuperación de información, cálculo de precios, redacción del itinerario). En todos los casos se exige al modelo una salida estructurada validada contra un esquema de datos predefinido, en lugar de interpretar texto libre, lo que evita errores de formato en las respuestas y hace que el comportamiento del sistema sea predecible y verificable.

\TFMSection{Integración con fuentes externas}

La información disponible en la base documental no siempre incluye precios actualizados
de vuelos y alojamientos. Por este motivo, el agente de precios se integra con SearchAPI,
un servicio externo que permite consultar los resultados de Google Flights y Google
Hotels mediante una API REST \parencite{searchapiFlights,searchapiHotels}.

Para la búsqueda de vuelos se realizan dos consultas independientes: una para el trayecto
de ida y otra para el de vuelta. Ambas peticiones se ejecutan en paralelo, reduciendo el
tiempo total de respuesta y permitiendo combinar compañías aéreas diferentes cuando
esta opción resulta más económica. Los resultados obtenidos incluyen información como
los aeropuertos, los horarios, la duración, las escalas y el precio de cada trayecto.

La búsqueda de alojamientos utiliza el destino, las fechas de entrada y salida y el número
de viajeros. A partir de la respuesta externa, el sistema selecciona y normaliza campos
como el nombre del alojamiento, la valoración, el precio y la información disponible
sobre la estancia.

Los resultados de vuelos y hoteles se transforman en modelos internos comunes antes
de ser utilizados por el agente de precios. De esta forma, el resto de la aplicación no
depende directamente de la estructura específica devuelta por SearchAPI y sería posible
sustituir o incorporar nuevos proveedores sin modificar la interfaz ni el flujo
multiagente.

Durante el desarrollo se estudió inicialmente la integración con las API de Amadeus a
través de un servidor MCP. Sin embargo, esta alternativa se descartó debido a los cambios
en la disponibilidad de su plataforma Self-Service y a la imposibilidad de mantener un
acceso gratuito adecuado para la prueba de concepto \parencite{amadeusSelfService}.
Por ello, se optó por SearchAPI, que permitió completar la integración de vuelos y
alojamientos con una menor complejidad de acceso.

Los precios y la disponibilidad obtenidos deben considerarse orientativos, ya que pueden
variar entre el momento de la consulta y la realización efectiva de la reserva. Por esta
razón, el sistema presenta varias alternativas al profesional y mantiene la selección final
como un punto obligatorio de intervención humana.

\TFMSection{Implementación de la API}
La API de persistencia expone los servicios necesarios para que la interfaz y el pipeline documental compartan una única fuente de verdad: autenticación de usuarios, alta y consulta de metadatos documentales, y alta, consulta y actualización del estado de las propuestas. El acceso de los usuarios finales se protege mediante un token de sesión, mientras que el pipeline documental se autentica con una cuenta de servicio independiente, de forma que las credenciales de un empleado nunca se comparten con procesos automáticos internos. Una propuesta solo puede modificarse por la persona que la creó, aunque su consulta desde el histórico está abierta a cualquier usuario autenticado, en línea con el uso compartido que hace de la herramienta un equipo comercial.

\TFMSection{Implementación del frontend}
La interfaz se ha construido como una aplicación conversacional sencilla, con dos pantallas: una de conversación, donde el profesional describe el viaje, ve la respuesta del sistema en tiempo real y toma las dos decisiones clave del proceso (selección de precios y revisión final de la propuesta); y otra de histórico, donde puede consultar, descargar o retomar cualquier propuesta generada anteriormente. Este diseño permite que un empleado sin conocimientos técnicos utilice un sistema multiagente complejo sin necesidad de entender su funcionamiento interno, a través de una conversación y de dos puntos de decisión claramente señalados.

El acceso a ambas pantallas queda protegido por la autenticación descrita en la sección anterior. La Figura~\ref{fig:frontend-login} muestra la pantalla de inicio de sesión, previa a cualquier otra funcionalidad.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{assets/frontend_login.png}
\caption{Pantalla de inicio de sesión del frontend.}
\label{fig:frontend-login}
\end{figure}

Tras autenticarse, el profesional llega por defecto a la pantalla de conversación (Figura~\ref{fig:frontend-orchestrator-vacio}), donde un único campo de texto invita a describir el viaje en lenguaje natural. La Figura~\ref{fig:frontend-orchestrator-prompt} muestra un ejemplo real de petición introducida por el usuario, que el agente orquestador (Sección~4.5) interpreta para extraer cliente, origen, destino, fechas e intereses antes de activar al resto de agentes.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{assets/frontend_orchestrator_vacio.png}
\caption{Pantalla de conversación (Orchestrator) al iniciar una sesión.}
\label{fig:frontend-orchestrator-vacio}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{assets/frontend_orchestrator_prompt.png}
\caption{Ejemplo de petición de un nuevo viaje introducida en lenguaje natural.}
\label{fig:frontend-orchestrator-prompt}
\end{figure}

Una vez generada, cada propuesta queda disponible en la pantalla de histórico (Figura~\ref{fig:frontend-dashboard}), que resume en formato tabular el cliente, el origen, el destino, las fechas, los intereses y el estado de cada propuesta creada por el equipo. Al seleccionar una propuesta de la lista, la interfaz despliega su contenido completo --itinerario día a día y desglose de precio-- (Figura~\ref{fig:frontend-dashboard-detalle}), desde donde el profesional puede descargarla en formato PPTX o enviarla de nuevo al sistema para iterar sobre ella.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{assets/frontend_dashboard.png}
\caption{Pantalla de histórico (Dashboard) con las propuestas generadas por el equipo.}
\label{fig:frontend-dashboard}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{assets/frontend_dashboard_detalle.png}
\caption{Detalle de una propuesta: itinerario día a día y desglose de precio.}
\label{fig:frontend-dashboard-detalle}
\end{figure}

Además de estas dos pantallas, el flujo se detiene en los dos puntos de interacción humana en el bucle descritos en la Sección~3.7. El primero ocurre tras la consulta de precios (Figura~\ref{fig:frontend-seleccion-precios}): la interfaz presenta las opciones de vuelo de ida y de vuelta, y de alojamiento, junto con su origen (documentación interna o SearchAPI), puntuación y puntos de interés cercanos al hotel, para que el profesional seleccione manualmente una opción de cada categoría antes de continuar; un checkbox adicional permite indicar que el cliente se desplazará por sus propios medios, omitiendo así la reserva de vuelo.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{assets/frontend_seleccion_precios.png}
\caption{Selección manual de vuelos y hotel (primer punto de revisión humana).}
\label{fig:frontend-seleccion-precios}
\end{figure}

El segundo punto de revisión ocurre tras generar la propuesta comercial completa (Figura~\ref{fig:frontend-revision-flyer}): la interfaz muestra el itinerario y el precio final ya integrados, y ofrece cuatro acciones explícitas --aprobar y publicar, rechazar indicando comentarios de corrección en un campo de texto libre, descartar o guardar como borrador-- que se corresponden directamente con los estados de revisión almacenados en el estado compartido del grafo (Sección~\ref{sec:impl-multiagente}).

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{assets/frontend_revision_flyer.png}
\caption{Revisión final de la propuesta comercial (segundo punto de revisión humana).}
\label{fig:frontend-revision-flyer}
\end{figure}

\TFMSection{Despliegue e Infra en AWS}
El pipeline documental se despliega como funciones independientes sobre AWS Lambda, cada una con sus dependencias empaquetadas en capas reutilizables, lo que mantiene cada función ligera y facilita su actualización individual. La API de persistencia se despliega también como contenedor Lambda, expuesta a través de una API Gateway. El orquestador multiagente y la interfaz, al ejecutarse en un mismo proceso de larga duración, se despliegan como un único contenedor en una instancia con Docker, en lugar de como función sin servidor. Toda la infraestructura utiliza en la actualidad servicios gestionados de almacenamiento, base de datos y búsqueda vectorial de AWS, configurados manualmente en el momento de escribir esta memoria; la ausencia de una definición de infraestructura como código es una limitación conocida del proyecto, recogida como trabajo futuro en el Capítulo 8.

\TFMSection{Seguridad y gestión de errores}
El sistema incorpora varios mecanismos para tolerar fallos sin interrumpir la experiencia del profesional: reintentos automáticos ante errores transitorios de los proveedores de modelos de lenguaje, y una degradación controlada cuando el sistema RAG o las fuentes externas de precios no están disponibles, de forma que el flujo continúa (por ejemplo, sin contexto documental o sin precios de una fuente) en lugar de interrumpirse por completo. Esta decisión es especialmente relevante en un sistema orientado a un caso de uso comercial, donde una propuesta incompleta pero disponible resulta más útil que un error que bloquee todo el proceso.
